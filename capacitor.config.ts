import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Food_Services',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
